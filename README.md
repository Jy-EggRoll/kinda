# Kinda - 交互式学习伴侣

“拒绝无效刷课，从**被动观看**走向**主动掌握**”

## 项目简介

**Kinda (会了吗)** 是一个创新的交互式学习工具，旨在解决传统视频学习“只看不练”的痛点。它利用先进的多模态大模型技术，将编程和机器学习等复杂的教学视频，智能拆解为可操作的互动学习单元。

通过深度理解视频中的视觉画面与语音讲解，Kinda 能够提取核心知识点并生成针对性的练习题。配合“双向锚点交互”与即时反馈机制，它搭建了一座连接原始知识与实际应用之间的桥梁，让学习过程变得更加高效、有趣且可衡量。

## 核心特色

### 🤖 **AI 驱动的交互式学习**

* **多模态深度理解**：基于 **Qwen2.5-VL-72B-Instruct** 模型，联合分析视频视觉信息与语音/文本内容。
* **知识点提炼**：自动识别易错点、难点与薄弱点，将被动观看转化为主动思考。
* **即时反馈**：系统自动判定答案，并在作答后提供建设性的解析与引导，帮助快速填补知识盲区。

### 🔗 **视频与题目“双向锚点交互”**

* **点击定位**：题目卡片右上角设有“查看讲解”锚点，点击即可让左侧视频跳转至该知识点出现的精确时间（如 00:08）并自动播放。
* **进度联动**：拖动视频进度条时，右侧对应的题目卡片会自动高亮（Highlight），实时提示“当前的讲解对应这道题”。

### 📊 **全方位数据追踪与导出**

* **可视化报告**：基于 `html2canvas` 技术，一键生成精美的学习成就海报。
* **数据可视化**：直观展示得分、正确率及错题数量，方便用户保存分享或掌握每日学习进度。
* **庆祝时刻**：答对题目时触发 Canvas 粒子烟花特效与音效反馈，提供满满的情绪价值。

### 🔍 **前端即时搜索与筛选**

* **实时过滤**：支持按题型（填空/选择/判断）或状态（错题）进行毫秒级筛选。
* **关键词高亮**：输入搜索词时，题目列表即时过滤并高亮匹配内容，复习效率倍增。

### 🎛️ **按需定制的学习粒度**

* **自主掌控节奏**：通过数字步进器精确控制生成题目的数量（1-20题）。
* **场景化适配**：无论是碎片时间的 3 题快测，还是深度复习的 20 题特训，Prompt 参数均能动态适配你的时间预算。

## 技术架构

```text
Kinda/
├── server.js                # Express 后端服务器 (API & 静态资源托管)
├── index.html               # 单页应用入口 (HTML5 + Tailwind CDN)
├── js/                      # 前端核心逻辑
│   ├── app.js               # 主应用控制器 (事件编排、轮询逻辑)
│   ├── ai.js                # AI 服务接口 (视频上传、生成请求)
│   └── ui.js                # 界面交互 (渲染、动画、报告生成)
├── assets/                  # 静态资源 (图片)
├── audio/                   # 音效文件 (反馈提示音)
├── uploads/                 # 临时视频存储目录
├── api                      # 后端密钥配置文件 (非 git 追踪)
└── package.json             # 项目依赖管理

```

## 技术栈

| 类别 | 技术选型 |
| --- | --- |
| **后端框架** | Express.js |
| **前端基础** | HTML5 + Vanilla JavaScript (ES6+) |
| **样式系统** | Tailwind CSS (CDN) |
| **配色主题** | Catppuccin Latte (CDN) |
| **AI 模型** | Qwen2.5-VL-72B-Instruct (via OpenAI SDK) |
| **媒体处理** | FFmpeg (fluent-ffmpeg) |
| **工具库** | html2canvas, Axios, Multer |
| **包管理** | pnpm |

> **注意**：本项目坚持“极致原生”原则，前端零框架依赖（无 React/Vue），样式完全通过 CDN 引入，确保轻量与极速加载。

## 快速开始

### 环境要求

* Node.js 18+
* pnpm
* 系统需支持 FFmpeg (项目已集成自动安装程序，但在某些环境下可能需要手动配置)

### 安装运行

```bash
# 1. 克隆项目
git clone https://github.com/your-org/kinda.git

# 2. 安装依赖
pnpm install

# 3. 配置密钥 (在根目录创建名为 'api' 的文件)
# 格式参考下方“环境变量”章节

# 4. 启动服务
pnpm start
# 服务默认运行在 http://localhost:12345

```

### 环境变量 / 配置文件

项目根目录下需创建一个名为 `api` 的文件（或使用 `.env`），用于配置大模型接口信息。
`api` 文件内容格式如下：

```properties
apikey=sk-xxxxxxxxxxxxxxxxxxxxxxxx
baseurl=https://www.sophnet.com/api/open-apis/v1
model=Qwen2.5-VL-72B-Instruct

```


## 页面导航

本项目为单页应用 (SPA)，主要包含以下交互视图：

| 视图模块 | 描述 |
| --- | --- |
| **左侧面板** | **文本输入 / 视频上传区**。支持折叠，包含文件拖拽上传与视频预览播放器。 |
| **右侧主区** | **卡片展示区**。动态渲染 AI 生成的交互式题目卡片。 |
| **顶部导航** | **工具栏**。包含进度条、全局搜索框、题型筛选器、状态指示灯。 |
| **模态窗口** | **学习报告**。展示答题数据，支持导出图片；**庆祝弹窗**。答对时的奖励反馈。 |

## 配色系统

基于 **Catppuccin Latte** (明亮模式) 的自然配色方案，兼顾美感与护眼：

| 色彩名称 | HEX | 用途 |
| --- | --- | --- |
| **Base** | `#1e1e2e` | 页面背景色 (深色模式适配) |
| **Surface0** | `#313244` | 卡片与面板背景 |
| **Mauve** | `#cba6f7` | 主标题与强调按钮 |
| **Blue** | `#89b4fa` | 选中状态、主操作按钮 |
| **Green** | `#a6e3a1` | 正确反馈、进度条 |
| **Red** | `#f38ba8` | 错误反馈 |
| **Yellow** | `#f9e2af` | 高亮提示、搜索匹配 |

## API 接口规范

### 视频分析

```http
// 1. 上传视频并创建任务
POST /api/upload-video
Content-Type: multipart/form-data
Body: { video: File, count: number }
Response: { taskId: string }

// 2. 轮询任务状态
GET /api/task/:id
Response: { 
  status: "processing" | "completed" | "failed", 
  progress: number,
  message: string,
  result?: Card[] 
}

```

### 文本生成

```http
// 纯文本生成题目
POST /api/generate
Content-Type: application/json
Body: { text: string, count: number }
Response: Card[]

```

## 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 许可证

[MIT License](https://www.google.com/search?q=LICENSE)

Copyright (c) 2025 EggRoll.

Made with ❤️ by Kinda Team